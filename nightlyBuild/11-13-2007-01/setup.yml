setup:
   mongrel: port=3000,env=development,addr=localhost

setup_cluster:
   mongrel: port=3000,env=development,addr=localhost
   mongrel: port=3001,env=development,addr=localhost
   mongrel: port=3002,env=development,addr=localhost
   mongrel: port=3003,env=development,addr=localhost
      